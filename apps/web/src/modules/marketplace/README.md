# Agent Service Marketplace Module

This module is backed by the `service-marketplace-action` edge function and new tables:
- `agent_services`
- `service_purchases`

Transactions apply a 20% treasury fee by design.
